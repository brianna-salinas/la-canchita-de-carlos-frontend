// Este archivo quedó duplicado con useCalendario.ts (que ya usa
// CalendarioPage.tsx). Se deja como re-export para no romper nada que
// lo importe, pero la fuente real de los hooks es useCalendario.ts.
export * from './useCalendario'
